# Normalize line endings: LF in the repository and on checkout.
* text=auto eol=lf

# Treat generated binaries as binary (no diff noise, no EOL conversion).
*.png binary
*.pdf binary
*.pkl binary
*.parquet binary
